/*
 * Page objects for the screens the auth and navigation tests touch. Page objects for the
 * workout and exercise screens land alongside the tests that drive them, so each one is
 * written against a real assertion rather than guessed at up front.
 */
export { LoginPage } from './login.page';
export { NavPage } from './nav.page';
export { UserSelectPage } from './user-select.page';
