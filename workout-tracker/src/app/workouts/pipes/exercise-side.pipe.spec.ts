import { ExerciseSidePipe } from './exercise-side.pipe';

describe('ExerciseSidePipe', () => {
  it('create an instance', () => {
    const pipe = new ExerciseSidePipe();
    expect(pipe).toBeTruthy();
  });
});
